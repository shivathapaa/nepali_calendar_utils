# Selectable dates

The `NepaliSelectableDates` predicate used to constrain which dates a UI or validation
layer accepts. The `before_`, `after_`, and `date_range` factories live on
`NepaliDateConverter`.

```{eval-rst}
.. automodule:: nepali_calendar_utils.nepali_selectable_dates
   :members:
   :show-inheritance:
```
