# API reference

The public surface of `nepali_calendar_utils`. Every name below is also re-exported
from the package root, so `from nepali_calendar_utils import NepaliDateConverter` works
regardless of the defining module.

```{toctree}
:maxdepth: 2

converter
data
holiday
selectable_dates
```
